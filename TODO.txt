- reinforce exit achived
- seq_len > 1: You give the LSTM an entire sequence (e.g. 10 timesteps) in one forward call. It “unrolls” over those 10 steps, returning outputs at each step. This is what allows the LSTM to learn dependencies from earlier timesteps within the same batch item.


New Methid
- LLM apprach
- Genetic algorithm learning
